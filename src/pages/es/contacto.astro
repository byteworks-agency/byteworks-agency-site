---
import Base from '../../layouts/Base.astro';

const title = 'Contacto — ByteWorks';
const description =
  'Conversemos sobre tu sitio web o idea de proyecto. Escríbenos por WhatsApp o usa nuestro formulario de contacto — soporte directo, rápido y personal.';

const phone = import.meta.env.PUBLIC_WHATSAPP_PHONE;
const message =
  import.meta.env.PUBLIC_WHATSAPP_MSG || 'Hola ByteWorks, estoy interesado en un sitio web.';
const wa = phone ? `https://wa.me/${phone}?text=${encodeURIComponent(message)}` : null;
---

<Base title={title} description={description} image="/og-image.png" lang="es">
  <section class="px-6 md:px-10 lg:px-16 max-w-3xl mx-auto py-16 md:py-24 text-center space-y-8">
    <h1 class="text-3xl md:text-5xl font-semibold">Trabajemos juntos</h1>
    <p class="text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
      ¿Listo para tener una web moderna, bilingüe y con mantenimiento incluido?
      Escríbenos directo por WhatsApp o envíanos un mensaje usando el formulario.
    </p>

    <div class="flex flex-col md:flex-row justify-center items-center gap-4 mt-8">
      {wa && (
        <a
          href={wa}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center justify-center px-6 py-3 rounded-md bg-black text-white dark:bg-white dark:text-black hover:opacity-90 transition"
        >
          Chatear por WhatsApp
        </a>
      )}
    </div>

    <!-- FORMULARIO -->
    <form
      id="contact-form"
      class="mt-12 text-left space-y-5 max-w-md mx-auto"
      onsubmit="event.preventDefault(); alert('Envío simulado — el backend se agregará pronto.')"
    >
      <div>
        <label for="nombre" class="block text-sm font-medium mb-1">Nombre</label>
        <input
          id="nombre"
          name="nombre"
          type="text"
          required
          placeholder="Tu nombre"
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        />
      </div>

      <div>
        <label for="email" class="block text-sm font-medium mb-1">Correo electrónico</label>
        <input
          id="email"
          name="email"
          type="email"
          required
          placeholder="tucorreo@ejemplo.com"
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        />
      </div>

      <div>
        <label for="mensaje" class="block text-sm font-medium mb-1">Mensaje</label>
        <textarea
          id="mensaje"
          name="mensaje"
          rows="5"
          required
          placeholder="Cuéntanos un poco sobre tu proyecto..."
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        ></textarea>
      </div>

      <button
        type="submit"
        class="w-full md:w-auto inline-flex items-center justify-center px-6 py-3 rounded-md bg-black text-white dark:bg-white dark:text-black hover:opacity-90 transition"
      >
        Enviar mensaje
      </button>
    </form>

    <div class="border-t border-gray-200 dark:border-gray-800 pt-10 mt-10 text-sm text-gray-600 dark:text-gray-400">
      <p>¿Prefieres una llamada o reunión? Podemos agendarla después de un breve chat.</p>
      <p class="mt-2 text-xs text-gray-500">
        Horario de atención: Lunes a Viernes, 9:00 AM – 6:00 PM (AST)
      </p>
    </div>
  </section>
</Base>
