---
import Base from '../../layouts/Base.astro';

const title = 'Contacto - ByteWorks';
const description =
  'Conversemos sobre tu sitio web o idea de proyecto. Escríbenos por WhatsApp - rápido y directo. Usa el formulario para iniciar la conversación.';

const phone = import.meta.env.PUBLIC_WHATSAPP_PHONE;
const message =
  import.meta.env.PUBLIC_WHATSAPP_MSG || 'Hola ByteWorks, estoy interesado en un sitio web.';
const wa = phone ? `https://wa.me/${phone}?text=${encodeURIComponent(message)}` : null;
---

<Base title={title} description={description} image="/og-image.png" lang="es">
  <section
    class="px-6 md:px-10 lg:px-16 max-w-3xl mx-auto py-16 md:py-24 text-center space-y-8"
    data-wa={phone || ''}
  >
    <h1 class="text-3xl md:text-5xl font-semibold">Trabajemos juntos</h1>
    <p class="text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
      ¿Listo para una web moderna, bilingüe y con mantenimiento incluido?
      Escríbenos directo por WhatsApp o envíanos un mensaje con el formulario.
    </p>

    <!-- Botón de WhatsApp removido para el nuevo embudo -->

    <form id="contact-form" class="mt-12 text-left space-y-5 max-w-md mx-auto">
      <div>
        <label for="name" class="block text-sm font-medium mb-1">Nombre / Marca / Empresa / Organización</label>
        <input
          id="name"
          name="name"
          type="text"
          required
          placeholder="ej. Juan Pérez / ACME S.A."
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        />
      </div>

      <div id="phone_wrap" class="">
        <label for="phone" class="block text-sm font-medium mb-1">Teléfono (WhatsApp)</label>
        <input
          id="phone"
          name="phone"
          type="tel"
          inputmode="tel"
          placeholder="ej. +1 868 775 9858"
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        />
      </div>

      <!-- Email removido por ahora y solo WhatsApp como método -->

      <div>
        <label for="message" class="block text-sm font-medium mb-1">Mensaje</label>
        <textarea
          id="message"
          name="message"
          rows="5"
          required
          placeholder="Cuéntanos un poco sobre tu proyecto..."
          class="w-full rounded-md border border-gray-300 dark:border-gray-700 bg-white dark:bg-black px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-black dark:focus:ring-white"
        ></textarea>
      </div>

      <input
        type="text"
        id="website"
        name="website"
        tabindex="-1"
        autocomplete="off"
        style="position:absolute;left:-10000px;top:auto;width:1px;height:1px;overflow:hidden;"
        aria-hidden="true"
      />

      <button
        id="submitBtn"
        type="submit"
        class="w-full md:w-auto inline-flex items-center justify-center px-6 py-3 rounded-md bg-black text-white dark:bg-white dark:text-black hover:opacity-90 transition"
      >
        Enviar mensaje
      </button>

      <p
        id="formStatus"
        role="status"
        aria-live="polite"
        class="text-sm text-gray-600 dark:text-gray-300 min-h-[1.5rem]"
        data-tone="idle"
      ></p>
    </form>

    <div class="border-t border-gray-200 dark:border-gray-800 pt-10 mt-10 text-sm text-gray-600 dark:text-gray-400">
      <p>¿Prefieres llamada o reunión? La agendamos después de un breve chat.</p>
      <p class="mt-2 text-xs text-gray-500">
        Horario: Lunes a Viernes, 9:00 AM - 6:00 PM (AST)
      </p>
    </div>
  </section>

  <script>
    (function () {
      const root = document.querySelector('section[data-wa]');
      const form = document.querySelector('#contact-form');
      const nameEl = document.querySelector('#name');
      const emailEl = document.querySelector('#email');
      const messageEl = document.querySelector('#message');
      const hpEl = document.querySelector('#website');
      const btn = document.querySelector('#submitBtn');
      // preferencia fija a WhatsApp
      const statusEl = document.querySelector('#formStatus');
      if (!form || !nameEl || !messageEl || !hpEl) return;

      let WA_NUMBER = (root && root.dataset ? (root.dataset.wa || '').trim() : '');
      WA_NUMBER = (WA_NUMBER || '').replace(/\D+/g, '');

      const phoneEl = document.querySelector('#phone');

      form.addEventListener('submit', async (ev) => {
        ev.preventDefault();

        const name = nameEl.value.trim();
        const email = '';
        const phone = phoneEl ? phoneEl.value.trim() : '';
        const msg = messageEl.value.trim();
        const hp = hpEl.value.trim();

        if (hp) return;
        const setStatus = (message, tone = 'info') => {
          if (!statusEl) return;
          statusEl.textContent = message;
          statusEl.dataset.tone = tone;
          statusEl.classList.remove(
            'text-red-600',
            'dark:text-red-400',
            'text-green-600',
            'dark:text-green-400',
            'text-amber-600',
            'dark:text-amber-400'
          );

          if (!message) {
            statusEl.dataset.tone = 'idle';
            return;
          }

          const toneClasses =
            tone === 'error'
              ? ['text-red-600', 'dark:text-red-400']
              : tone === 'success'
                ? ['text-green-600', 'dark:text-green-400']
                : ['text-amber-600', 'dark:text-amber-400'];

          statusEl.classList.add(...toneClasses);
        };
        if (!name || !msg || !phone) {
          setStatus('Por favor completa los campos requeridos.', 'error');
          return;
        }

        const original = btn ? btn.textContent : null;
        setStatus('Enviando...', 'info');
        if (btn) { btn.disabled = true; btn.textContent = 'Enviando...'; }

        // Abrir WhatsApp primero (dentro del gesto del usuario) para evitar bloqueos de popups
        if (!WA_NUMBER) {
          setStatus('WhatsApp no está configurado. Intenta más tarde.', 'error');
          if (btn) { btn.disabled = false; btn.textContent = original || 'Enviar mensaje'; }
          return;
        }
        const waText =
          `¡Hola ByteWorks!\n\n` +
          `Nuevo contacto:\n` +
          `Nombre: ${name}\n` +
          (phone ? `Teléfono: ${phone}\n` : '') +
          `Mensaje:\n${msg}`;
        const waUrl = `https://wa.me/${encodeURIComponent(WA_NUMBER)}?text=${encodeURIComponent(waText)}`;
        try {
          const a = document.createElement('a'); a.href = waUrl; a.target = '_blank'; a.rel = 'noopener';
          document.body.appendChild(a); a.click(); a.remove();
        } catch {
          try { window.open(waUrl, '_blank'); } catch { window.location.href = waUrl; }
        }

        // Registrar el lead sin bloquear la navegación
        try {
          await fetch('/api/contact', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              name,
              phone,
              message: msg,
              lang: 'es',
              sourceUrl: window.location.href,
              preference: 'whatsapp',
            }),
            keepalive: true,
          });
        } catch {}

        nameEl.value = '';
        if (phoneEl) phoneEl.value = '';
        messageEl.value = '';
        hpEl.value = '';
        if (btn) { btn.disabled = false; btn.textContent = original || 'Enviar mensaje'; }
        setStatus('¡Gracias! Recibimos tu solicitud y te contactaremos pronto.', 'success');
      });
    })();
  </script>
</Base>
